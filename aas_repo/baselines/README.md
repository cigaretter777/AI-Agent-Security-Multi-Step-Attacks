# Baselines

`secret_marker.py` is an earlier marker-dependent experimental baseline retained to document the solution's evolution.
It is not the final competition strategy. Keep all executions inside the official/authorized sandbox described in
`SECURITY.md`.
